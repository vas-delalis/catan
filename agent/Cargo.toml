[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.20.1", features = [
	"metrics",
	"tch",
	"train",
	"tui",
	"vision",
] }
catan = { path = "../catan" }
rand = "0.9.2"
rand_distr = "0.5.1"

[dev-dependencies]
criterion = "0.8.1"
